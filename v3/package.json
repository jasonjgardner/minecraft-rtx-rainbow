{
  "name": "rainbow",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun ./scripts/builder/app/build.ts",
    "compile": "bun ./scripts/builder/scripts.ts",
    "deploy": "bun ./scripts/builder/deploy.ts",
    "format": "prettier -w ./scripts/**/*.ts",
    "dist": "bun run format && bun run build && bun run compile && bun run deploy"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/fs-extra": "^11.0.4",
    "prettier": "^3.3.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@minecraft/server": "^1.11.0",
    "fs-extra": "^11.2.0",
    "imagescript": "^1.3.0"
  }
}